{
  "name": "ruwa-mcp",
  "version": "0.3.0",
  "description": "MCP server exposing ruwa (RUWA — Rust WhatsApp) as agent tools",
  "type": "module",
  "bin": { "ruwa-mcp": "dist/index.js" },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "setup-search": "node scripts/setup-search.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  }
}
